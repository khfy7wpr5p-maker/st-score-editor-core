name: P08-E4 professional artifact WebKit

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  professional-webkit:
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-professional-webkit-stage.yml
    with:
      stage: p08-e4
