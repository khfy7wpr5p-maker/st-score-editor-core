name: P10-1 renderer qualification WebKit

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  renderer-qualification-webkit:
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-professional-webkit-stage.yml
    with:
      stage: p10-1-renderer
