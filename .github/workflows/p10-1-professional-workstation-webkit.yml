name: P10-1 professional workstation WebKit

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  professional-workstation-webkit:
    permissions:
      contents: read
    uses: ./.github/workflows/reusable-professional-webkit-stage.yml
    with:
      stage: p10-1
